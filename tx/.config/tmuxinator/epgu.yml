# /home/slava/.config/tmuxinator/epgu.yml

name: epgu
root: ~/code/epgu

startup_window: editor

pre_window:
  - source setenv.sh
  - source ~/.venv/epgu_venv/bin/activate
  - clear
  - echo "setenv, venv activated"

windows:
  - editor:
      panes:
        - nvim:
            - cd src/gov
        - nvim:
            -
