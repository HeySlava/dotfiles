# /home/slava/.config/tmuxinator/airflow.yml

name: airflow
root: ~/code/airflow_course

pre_window: source ~/.venv/airflow_course_venv/bin/activate
startup_window: editor

windows:
  - editor:
      panes:
        - nvim:
          -
        - concole:
          -
